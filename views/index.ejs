<!DOCTYPE html>
<html lang="en">
    <!-- partials template with ejs => head.ejs/nav.ejs/foot.ejs -->
    <%- include('./partials/head.ejs') %>
<body>
    <%- include('./partials/nav.ejs') %>
    <div class="blogs content">
        <h2>All Blogs - Server side rendering with EJS</h2>
        <% if(blogs.length > 0){ %>
            <% blogs.forEach(blog => { %>
                <h3 class="title"><%= blog.title %></h3>
                <p class="snipper"><%= blog.snippet %></p>
           <% }) %>
        <% } else { %>
            <h3>There are no blogs to display. . . . .</h3>
       <% } %>
    </div>
    <%- include('./partials/foot.ejs') %>
</body>
</html>